#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	long long a;
	cin >> a;
	cout << a * 78 / 100 << ' ' << a * 956 / 1000;
	return 0;
}