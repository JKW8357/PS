#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	long long n, r;
	cin >> n >> r;
	cout << (n + 1) + 2 * (r - 1) << '\n';

	return 0;
}